cache: true

language: scala

jdk:
  - oraclejdk8

script:
  - ./setup.sh
  - export SBT_OPTS="-Xmx2G -Xms512m -XX:+UseG1GC"
  - sbt clean compile test:compile

notifications:
  email:
    on_success: change
    on_failure: always
